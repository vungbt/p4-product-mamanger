// TODO: ProtectedRoute — props: allowedRoles: Role[]
// Chưa login → redirect login tương ứng
// Sai role (user vào admin) → redirect + toast "Không có quyền"

export default function ProtectedRoute() {
  return null;
}
