// TODO: Route config
// Storefront: /login, /shop, /cart — ProtectedRoute allowedRoles: ['user']
// Admin: /admin/login, /admin/dashboard, /admin/products... — allowedRoles: ['admin']

export default function AppRoutes() {
  return null;
}
