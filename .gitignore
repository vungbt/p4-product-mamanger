# dependencies
node_modules/

# lock files — không commit (trainee/CI tự install)
pnpm-lock.yaml
package-lock.json
yarn.lock

# build
dist/
*.tsbuildinfo
storybook-static/

# turbo / logs
.turbo/
*.log
npm-debug.log*
pnpm-debug.log*

# env
.env
.env.local
.env.*.local
.env.prod

# docker artifacts
*.tar.gz

# os / editor
.DS_Store
.idea/
.vscode/
*.swp

# local tooling
.tmp-img/

# scratch — files staged for manual deletion (AI tooling can't delete directly)
_to_delete/
