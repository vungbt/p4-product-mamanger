// Re-export @p4/api-client — trainee có thể import từ đây hoặc từ `@p4/api-client`
export {
  axiosClient,
  categoryKeys,
  configureApiClient,
  getAccessToken,
  getHeader,
  orderKeys,
  productKeys,
  queryKeysFactory,
  useApiMutation,
  useApiQuery,
} from '@p4/api-client';
