// Re-export @p4/api-client — trainees can import from here or from `@p4/api-client`
export {
  axiosClient,
  categoryKeys,
  configureApiClient,
  getAccessToken,
  getHeader,
  orderKeys,
  productKeys,
  queryKeysFactory,
  useApiMutation,
  useApiQuery,
} from '@p4/api-client';
