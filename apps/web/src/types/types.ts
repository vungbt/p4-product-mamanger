// Re-export types from the shared package (matches the API the mentor provided)
export type {
  ApiError,
  DashboardStats,
  LoginRequest,
  LoginResponse,
  Order,
  OrderItem,
  Product,
  ProductInput,
  Role,
  User,
} from '@p4/shared';
